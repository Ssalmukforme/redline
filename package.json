{
  "name": "redline",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --configLoader native",
    "build": "vite build --configLoader native",
    "preview": "vite preview --host 0.0.0.0 --configLoader native",
    "test": "node --test tests/*.test.js",
    "bench": "node tests/police-bench.mjs --tight && node tests/escape-bench.mjs"
  },
  "dependencies": {
    "three": "0.184.0"
  },
  "devDependencies": {
    "vite": "7.3.6"
  }
}
